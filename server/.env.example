# Banco de dados
DATABASE_URL="postgresql://..."
PORT=3001
CORS_ORIGIN=http://localhost:3000
JWT_SECRET=sua-chave-secreta-longa-e-aleatoria
JWT_EXPIRES_IN=365d

# E-mail (Esqueci minha senha) - obrigatório para enviar token por e-mail
# Exemplo Gmail: use "Senha de app" (conta Google > Segurança > Senhas de app)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-de-app
EMAIL_FROM=seu-email@gmail.com

# URL do frontend (para o link no e-mail)
APP_URL=http://localhost:3000

services:
  # Backend API
  - type: web
    name: acervo-digital-api
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        value: # Configure manualmente no Render com a connection string do Supabase
      - key: CORS_ORIGIN
        fromService:
          name: acervo-digital-frontend
          type: web
          property: host

  # Frontend
  - type: web
    name: acervo-digital-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        fromService:
          name: acervo-digital-api
          type: web
          property: host


